{
  "name": "ts-pom-bdd-di",
  "version": "1.0.0",
  "private": true,
  "description": "BDD + Page Object Model + Playwright TypeScript + Dependency Injection (Fixtures) sample for ShopTodo",
  "scripts": {
    "test": "bddgen && playwright test",
    "test:headed": "bddgen && playwright test --headed",
    "test:debug": "bddgen && playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "playwright-bdd": "^7.0.0",
    "typescript": "^5.3.0"
  }
}
